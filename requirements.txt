# MailShield Basic — Python dependencies

# API
fastapi==0.111.0
uvicorn==0.29.0
python-jose[cryptography]==3.3.0   # JWT tokens
passlib[bcrypt]==1.7.4

# Database
asyncpg==0.29.0
sqlalchemy[asyncio]==2.0.30

# Task queue
celery==5.3.6
redis==5.0.4

# Email handling
aioimap==0.2.7                     # async IMAP with IDLE support

# HTTP client (blacklist queries)
httpx==0.27.0

# DNS lookups (SPF/DKIM/DMARC)
dnspython==2.6.1
checkdmarc==5.14.1

# NLP — typosquatting distance
python-Levenshtein==0.25.1

# Utilities
python-dotenv==1.0.1
pydantic==2.7.1
structlog==24.1.0                  # structured logging
